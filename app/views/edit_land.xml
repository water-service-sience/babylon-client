<Alloy>
	<Window class="container">
		
		
		<View class="line_element">
			<Label>名前</Label><TextField id="name"></TextField>
		</View>
		<!-- Note the ns attribute is used to change the default namespace. -->
        <View id="map" ns="Alloy.Globals.Map" onClick="onMapClicked"
            animate="true" regionFit="true" userLocation="true"
            mapType="1" >

            <!-- Place other child views here, such as an ImageView to add an overlay. -->

        </View>
        
    	<Button onClick="onOkClicked">保存</Button>
    	<Button onClick="onDeleteClicked" width="30%">削除</Button>
	</Window>
</Alloy>